spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:excalidraw_test}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-key-256-bits-long-for-unit-tests-only-not-production!!
    expiration-ms: 86400000
  cors:
    allowed-origins: http://localhost:5173
